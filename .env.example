# Arc CLI loads this file from its working directory.
BASE_URL=https://api.example.com/v1
MODEL=your-tool-capable-model-id
API_KEY=
# If a system proxy cannot reach the API, bypass it for this host:
# NO_PROXY=api.example.com
